// apap-engine root build.
//
// このプロジェクトはモジュール単位で `build-logic`（includeBuild）の Convention Plugin
// （apap.kotlin-common / apap.library / apap.application / apap.quality）を適用する。
// ルートには集約タスク以外のロジックを持たせない。
